<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Mesmo pacote dos projetos irmãos. Traz transitivamente:
         Microsoft.Extensions.AI (AIFunctionFactory, AIFunction),
         Microsoft.Agents.AI (AIAgent, AsAIAgent),
         Anthropic (AnthropicClient) -->
    <PackageReference Include="Microsoft.Agents.AI.Anthropic" Version="1.0.0-rc6" />
  </ItemGroup>

</Project>
